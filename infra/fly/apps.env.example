# Fly.io アプリ名（実環境に合わせて変更する）。
FLY_CLIENT_APP=history-quiz-client
FLY_BACKEND_APP=history-quiz-backend

# deploy 時に利用する fly.toml のパス。
FLY_CLIENT_CONFIG=infra/fly/client.fly.toml
FLY_BACKEND_CONFIG=infra/fly/backend.fly.toml

# Authentik を同時デプロイする場合のみ設定する（未利用なら空のままでよい）。
# FLY_AUTHENTIK_APP=history-quiz-authentik
# FLY_AUTHENTIK_CONFIG=infra/fly/authentik.fly.toml

# クライアントから backend へ接続する Fly 内部DNS。
# 例: history-quiz-backend.internal:50051
BACKEND_GRPC_ADDRESS=history-quiz-backend.internal:50051

# 任意設定（未設定時はスクリプト側の既定値を利用）
# BACKEND_GRPC_TIMEOUT_MS=3000
# BACKEND_GRPC_TLS=false
# OIDC_SCOPES=openid profile email
# GRPC_PROTO_ROOT=/app/proto
