# Authentik 用の環境変数（ローカル開発向け）
# 重要: 本ファイルはサンプル。実際は `.env` を作成して値を設定する。

# Authentik の暗号鍵（ランダムな長い文字列にする）
AUTHENTIK_SECRET_KEY=change-me-please

# 初期管理者ユーザー（bootstrap）
AUTHENTIK_BOOTSTRAP_PASSWORD=change-me-please
AUTHENTIK_BOOTSTRAP_EMAIL=admin@example.com

# Authentik が参照する DB（Authentik 用。アプリDBの Neon とは別）
AUTHENTIK_POSTGRES_PASSWORD=change-me-please
AUTHENTIK_POSTGRES_USER=authentik
AUTHENTIK_POSTGRES_DB=authentik

# Authentik のサーバー公開ポート
AUTHENTIK_HTTP_PORT=9000

# イメージタグ（ローカルは latest でもよいが、本番は固定推奨）
AUTHENTIK_IMAGE=ghcr.io/goauthentik/server:latest
