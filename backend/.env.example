# backend ローカル開発用の環境変数テンプレート。
# 実運用値は backend/.env に設定し、このファイルは直接編集しない。

# アプリDB（ローカル Postgres）の接続文字列。
DATABASE_URL=postgresql://historyquiz:historyquiz@127.0.0.1:5432/historyquiz?sslmode=disable

# gRPC サーバーの待ち受けポート。
PORT=50051
