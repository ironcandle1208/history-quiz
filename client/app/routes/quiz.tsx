// クイズ画面（取得→回答→判定→次へ）を提供するルート。
// NOTE: 実際の gRPC 呼び出しは server-only のクライアントを経由する（ブラウザから直接 gRPC は呼ばない）。

export default function QuizRoute() {
  return (
    <main>
      <h1>クイズ</h1>
      <p>未実装（後続タスクで loader/action を追加）</p>
    </main>
  );
}

